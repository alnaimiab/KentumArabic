# المساهمة في تعريب Kentum

<div dir="rtl">

أهلًا بك. **الترجمة مكتملة — 3,308 نصًا، منها 732 سطر حوار.** فالمساهمة اليوم ليست ملء فراغ، بل تحسين ما هو قائم:

- **صياغة أفضل** لنص يقرأ ركيكًا أو حرفيًا.
- **نكتة ضاعت.** كثير من أسماء المهام مراجع (‏`Claw & Order` تلاعب على «القانون والنظام»)، وترجمة كلماتها تلغي سببها. إن رأيت واحدة مسطَّحة فأخبرنا أو أصلحها.
- **خطأ معنى.** وقع منها اثنان بالفعل: `They See Me Phasing` تُرجمت «يرونني أطوّر» — قُرئت phasing كطور نمو.
- **نصوص جديدة** يضيفها الاستوديو في تحديث.

> **مهم:** أي نص غير مترجم يظهر بالإنجليزية ولا شيء ينكسر. فلا تتردد في إرسال إصلاح جزئي.

## ما تحتاجه

- نسخة من اللعبة على Steam.
- محرر نصوص يدعم UTF-8 (VS Code أو Notepad++). **لا تستخدم Excel** — فهو يفسد الترميز وعلامات الجدولة.
- Python 3 (اختياري، لتشغيل أداة الفحص).

## الخطوات

### ١. ثبّت التعريب
اتبع [دليل التثبيت](docs/install-ar.md).

### ٢. استخرج النصوص الأصلية
شغّل اللعبة، وبعد وصولك للقائمة الرئيسية اضغط **Ctrl+F12**، ثم ابدأ لعبة أو حمّل حفظًا واضغطها مرة أخرى (نصوص الحوار لا تتوفر قبل تحميل قاعدة بياناتها).

تجد الملفات في:
```
<مجلد اللعبة>\BepInEx\plugins\KentumArabic\_dump\
```

> ⚠️ **لا ترفع مجلد `_dump` إلى المستودع.** فيه النص الإنجليزي والإسباني الأصلي، وهو ملك للاستوديو ومحمي بحقوق النشر. المستودع ينشر المفاتيح والعمود العربي فقط.

### ٣. عدّل
ملفات الترجمة في `content/strings/` وفيها عمودان فقط: `key` و`ar`. أما ملفات `_dump/` فتحوي الأعمدة المرجعية (الإنجليزي والإسباني والمتحدث) — قارن بينها وأنت تعمل.

الأعمدة:

| العمود | معناه |
|---|---|
| `key` | مفتاح النص — **لا تغيّره أبدًا** |
| `ar` | ترجمتك — هذا العمود الوحيد الذي تعدّله |
| `en` | النص الإنجليزي الأصلي (مرجع) |
| `es` | الترجمة الإسبانية الرسمية (مرجع ممتاز — يُظهر كيف تعامل مترجمو الاستوديو مع القيود) |
| `speaker` / `listener` | من يتكلم ومع من (في ملف الحوار) |
| `flags` | `format` = يحتوي عناصر نائبة، `richtext` = يحتوي وسوم تنسيق |

### قواعد الترجمة

**اكتب العربية عادية.** لا تشكّل الحروف يدويًا ولا تعكس النص ولا تستخدم أدوات مثل «تدوين». الإضافة تتولى التشكيل والاتجاه أثناء العرض. النص المشكَّل مسبقًا غير قابل للمراجعة ولا للتعديل لاحقًا.

- ✅ `مرحبا بك في كنتوم`
- ❌ `ﻡﻮﺘﻨﻛ ﻲﻓ ﻚﺑ ﺎﺒﺣﺮﻣ`

**لا تشكيل إطلاقًا — وهذا قيد تقني لا ذوقي.** ‏TextMeshPro لا يضبط مواضع الحركات: يرسم الحركة عند موضع القلم لا مرتكزة على شكل الحرف. فتنزاح انزياحًا مرئيًا على الحروف الضيقة كالراء، ويختلف الانزياح من كلمة لأخرى، فتبدو الحروف كأنها لا تستقر. **لا خط يصلح هذا.** إن احتجت رفع لبس فاستعمل الإملاء: «كينت» بالياء لا «كِنت» بالكسرة.

**تجنّب أفعال الأمر التي تلتبس بلا تشكيل.** بلا حركة، هذه كلمات أخرى تمامًا:

| لا تكتب | لأنها تُقرأ | اكتب |
|---|---|---|
| ابن | الوَلَد | **شيد** |
| سلم | السلام / السُّلَّم | **أودع** |
| أنه | «أنَّه» | **أكمل** |

> وإن بدّلت فعلًا فأعد قراءة ما بعده: **«أودع» يتعدى بـ«في» لا بـ«إلى»**. استبدال الفعل وحده يترك حرف جر الفعل السابق، فينتج «أودع البذور **إلى** المخزون». ولن يمسك هذا أي فحص آلي: الوسوم سليمة والعناصر النائبة سليمة، والخطأ نحوي فقط.

**حافظ على العناصر النائبة كما هي بالضبط.** `{0}` و`{1}` تُستبدل بقيم أثناء اللعب. حذفها أو تغييرها يسبب تعطلًا.
- الإنجليزية: `Collected {0} of {1}`
- ✅ `تم جمع {0} من {1}`
- ❌ `تم جمع من` — العنصر النائب ضاع
- ❌ `تم جمع {٠}` — الأرقام العربية لا تعمل هنا

**حافظ على وسوم التنسيق متوازنة.** `<color=#ff0000>خطر</color>` — كل وسم مفتوح يحتاج إغلاقًا.

**السطر الجديد يُكتب `\n` حرفيًا** (شرطة مائلة ثم n)، لا سطرًا فعليًا. كل نص يجب أن يبقى في سطر واحد.

**الخلية الفارغة تعني "لم يُترجم بعد"** وستظهر بالإنجليزية. هذا آمن ومقصود.

**التزم بالمسرد.** [docs/glossary.md](docs/glossary.md) يحمل القرارات وأسبابها، و[content/terms.tsv](content/terms.tsv) يحمل الجزء القابل للفحص آليًا. **يفرضه CI**، فمصطلح بصياغتين يوقف الدمج. إن قررت توحيد مصطلح جديد، حدّث الملفين معًا.

> مصطلح واحد بصياغتين هو أوضح دليل على أن أحدًا لا يراقب النص. ظهر «Cognitive Fluid» عندنا «سائل معرفي» على العنصر و«سائل إدراكي» في الحوار عن العنصر نفسه — ومن يقرأ الاثنين يلاحظ فورًا.

### ٤. جرّب فورًا
اضغط **Ctrl+Alt+R** داخل اللعبة لإعادة تحميل ملفات الترجمة دون إعادة تشغيل. غيّر، احفظ، اضغط، شاهد.

استخدم **Ctrl+Alt+L** مع العربية مختارة: كل نص غير مترجم يظهر بعلامة حمراء — تقرير تغطية مباشر على الشاشة.

### ٥. افحص قبل الإرسال

أربعة فحوص، تعمل كلها تلقائيًا على كل Pull Request:

```bash
python tools/validate_tsv.py content/strings          # عناصر نائبة، وسوم، مفاتيح مكررة
python tools/check_terms.py content/strings           # اتساق المصطلحات
python tools/strip_tashkeel.py content/strings --check # تشكيل لا يستطيع المحرك ضبطه
dotnet run --project tools/ShaperTest -c Release -- content/strings   # التشكيل الحرفي لكل نص
```

الأخير يمرّر **كل نص مشحون** عبر المعالج الحقيقي. أُضيف بعد أن كسر عنصر نائب معكوس (`}0{`) شاشة التحميل في اللعبة: الحالات المنتقاة يدويًا كانت تفحص وسوم التنسيق، لكن أيًا منها لم يصادف أن يحوي عنصرًا نائبًا.

### ٦. أرسل Pull Request
اشرح ما ترجمته. لا بأس بملف ناقص.

## أين النصوص

| الملف | العدد | ملاحظة |
|---|---|---|
| `dialogue.tsv` | 732 | الحوار. أهم ما فيه النبرة: كينت ساخر، وO.R.B. جاف دقيق |
| `ui_UI.tsv` | 507 | الواجهة والقوائم |
| `ui_Tech.tsv` | 450 | شجرة التقنيات |
| `ui_Item.tsv` | 407 | العناصر وأوصافها |
| `ui_BaseExpansion.tsv` | 400 | المباني |
| `ui_QuestData.tsv` | 166 | **أكثفها مراجعَ ونكاتًا** — وأكثرها ظهورًا على الشاشة |
| الباقي | 646 | أحياء ونباتات ومناطق وتعزيزات |

## للمبرمجين

بنية المشروع وطريقة البناء في [README](README.md#for-developers). أهم ما ينبغي معرفته:

- **التشكيل يُختبر دون تشغيل اللعبة**: `dotnet run --project tools/ShaperTest -c Release -- content/strings`
- **التشكيل يحدث وقت العرض** عبر `ITextPreprocessor` — وهو خطاف TMP الرسمي، يُستدعى على النص **بعد** تركيبه. لذلك تُحفظ البيانات عربية منطقية عادية، ويكفي تحسين المعالج لإعادة تشكيل كل شيء مجانًا.
- **قبل اقتراح خط جديد:** استخرج ما تحتاجه الترجمة فعلًا بـ`ShaperTest --glyphs` ثم افحص الخط عليه. تغطية نطاق Presentation Forms-B وحدها مضللة — معظمه حروف فارسية وأردية لا تنتجها العربية، ولهذا ترفض خطوطًا صالحة وتقبل أخرى تعرض مربعات فارغة.
- كل ترقيع Harmony معزول داخل `try/catch` — الإضافة يجب ألا تعطّل اللعبة أبدًا مهما فشل.
- لا تُستدعى أوامر وحدة تحكم اللعبة إطلاقًا: مسار الغش يعطّل الإنجازات.
- الاتجاه: نعكس **النص فقط**، لا تخطيط الواجهة.

</div>
