# المساهمة في تعريب Kentum

<div dir="rtl">

أهلًا بك. اللعبة تحتوي على حوالي **٤٠٠٠ نص** — أي أن المساهمات مرحّب بها بشدة، ولا تحتاج أن تكون مبرمجًا للمشاركة.

> **مهم:** الترجمة الجزئية صالحة تمامًا. أي نص لم يُترجم بعد يظهر بالإنجليزية ولا شيء ينكسر. لست مضطرًا لإنهاء ملف كامل.

## ما تحتاجه

- نسخة من اللعبة على Steam.
- محرر نصوص يدعم UTF-8 (VS Code أو Notepad++). **لا تستخدم Excel** — فهو يفسد الترميز وعلامات الجدولة.
- Python 3 (اختياري، لتشغيل أداة الفحص).

## الخطوات

### ١. ثبّت التعريب
اتبع [دليل التثبيت](docs/install-ar.md).

### ٢. استخرج النصوص الأصلية
شغّل اللعبة، وبعد وصولك للقائمة الرئيسية اضغط **Ctrl+F12**، ثم ابدأ لعبة أو حمّل حفظًا واضغطها مرة أخرى (نصوص الحوار لا تتوفر قبل تحميل قاعدة بياناتها).

تجد الملفات في:
```
<مجلد اللعبة>\BepInEx\plugins\KentumArabic\_dump\
```

> ⚠️ **لا ترفع مجلد `_dump` إلى المستودع.** فيه النص الإنجليزي والإسباني الأصلي، وهو ملك للاستوديو ومحمي بحقوق النشر. المستودع ينشر المفاتيح والعمود العربي فقط.

### ٣. ترجم
انسخ ملف `_dump/ui_UI.tsv` مثلًا إلى `content/strings/`، وترجم عمود `ar`.

الأعمدة:

| العمود | معناه |
|---|---|
| `key` | مفتاح النص — **لا تغيّره أبدًا** |
| `ar` | ترجمتك — هذا العمود الوحيد الذي تعدّله |
| `en` | النص الإنجليزي الأصلي (مرجع) |
| `es` | الترجمة الإسبانية الرسمية (مرجع ممتاز — يُظهر كيف تعامل مترجمو الاستوديو مع القيود) |
| `speaker` / `listener` | من يتكلم ومع من (في ملف الحوار) |
| `flags` | `format` = يحتوي عناصر نائبة، `richtext` = يحتوي وسوم تنسيق |

### قواعد الترجمة

**اكتب العربية عادية.** لا تشكّل الحروف يدويًا ولا تعكس النص ولا تستخدم أدوات مثل "تدوين". الإضافة تتولى التشكيل والاتجاه أثناء العرض. النص المشكَّل مسبقًا غير قابل للمراجعة ولا للتعديل لاحقًا.

- ✅ `مرحبا بك في كنتوم`
- ❌ `ﻡﻮﺘﻨﻛ ﻲﻓ ﻚﺑ ﺎﺒﺣﺮﻣ`

**حافظ على العناصر النائبة كما هي بالضبط.** `{0}` و`{1}` تُستبدل بقيم أثناء اللعب. حذفها أو تغييرها يسبب تعطلًا.
- الإنجليزية: `Collected {0} of {1}`
- ✅ `تم جمع {0} من {1}`
- ❌ `تم جمع من` — العنصر النائب ضاع
- ❌ `تم جمع {٠}` — الأرقام العربية لا تعمل هنا

**حافظ على وسوم التنسيق متوازنة.** `<color=#ff0000>خطر</color>` — كل وسم مفتوح يحتاج إغلاقًا.

**السطر الجديد يُكتب `\n` حرفيًا** (شرطة مائلة ثم n)، لا سطرًا فعليًا. كل نص يجب أن يبقى في سطر واحد.

**الخلية الفارغة تعني "لم يُترجم بعد"** وستظهر بالإنجليزية. هذا آمن ومقصود.

**التزم بالمسرد.** أسماء الأعلام والمصطلحات المتكررة في [docs/glossary.md](docs/glossary.md) — أضف إليه ما تقرره حتى تبقى الترجمة متسقة بين المساهمين.

### ٤. جرّب فورًا
اضغط **Ctrl+Alt+R** داخل اللعبة لإعادة تحميل ملفات الترجمة دون إعادة تشغيل. غيّر، احفظ، اضغط، شاهد.

استخدم **Ctrl+Alt+L** مع العربية مختارة: كل نص غير مترجم يظهر بعلامة حمراء — تقرير تغطية مباشر على الشاشة.

### ٥. افحص قبل الإرسال
```bash
python tools/validate_tsv.py content/strings
```
تكشف الأداة العناصر النائبة الضائعة والوسوم غير المتوازنة والمفاتيح المكررة. وتعمل تلقائيًا على كل Pull Request.

### ٦. أرسل Pull Request
اشرح ما ترجمته. لا بأس بملف ناقص.

## الترتيب المقترح للعمل

الأكثر ظهورًا على الشاشة أولًا:

1. `UI_` — الواجهة والقوائم (٥٤١ مفتاحًا)
2. `Item_` — العناصر (٦٨٢)
3. `Tech_` — شجرة التقنيات (٨٣٩)
4. `World_` و`Enemy_` و`Char_` و`Player_` وغيرها (~٣٨٩)
5. الحوارات (~٨٧٨ + ~٨٥٠ ردًا)

## للمبرمجين

بنية المشروع وطريقة البناء في [README](README.md#for-developers). أهم ما ينبغي معرفته:

- **التشكيل يُختبر دون تشغيل اللعبة**: `dotnet run --project tools/ShaperTest -c Release`
- كل ترقيع Harmony معزول داخل `try/catch` — الإضافة يجب ألا تعطّل اللعبة أبدًا مهما فشل.
- لا تُستدعى أوامر وحدة تحكم اللعبة إطلاقًا: مسار الغش يعطّل الإنجازات.
- الاتجاه: نعكس **النص فقط**، لا تخطيط الواجهة.

</div>
